<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Base.Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
    <style name="EditTextStyle">
        <item name="android:padding">@dimen/spacing_tiny</item>
        <item name="android:drawablePadding">@dimen/spacing_tiny</item>
        <item name="android:textSize">@dimen/font_small</item>
        <item name="android:textColor">@android:color/black</item>
        <item name="android:background">@drawable/drawable_edittext_style</item>
        <item name="android:minHeight">@dimen/button_height_short</item>
    </style>

    <style name="EditTextStyle.">
        <item name="android:textViewStyle">@color/colorPrimary</item>
    </style>
    <style name="Widget.App.TextView"
        parent="Widget.AppCompat.EditText">???</style>
    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>
